# TODO

Last checked: 2026-05-09.

## Done

- [x] Make the full local test suite pass.
  - `moon check`
  - `moon test` reports 298 passed, 0 failed.
- [x] Cover parser behavior with generated JSONTestSuite cases.
  - Fixtures are generated by `tools/update_json_testsuite.py`.
  - Current upstream commit: `1ef36fa01286573e846ac449e8683f8833c5b26a`.
- [x] Re-export `Value` enough for top-level incremental parsing tests.
  - `src/top.mbt` publicly re-exports `Value`, `ContinueValue`, and
    `FinishValue`.
  - `src/tests/incr.mbt` can match `Continue` and `Finish` through
    `jinser/json`.

## Open

- [ ] Add a usable benchmark suite.
  - Include whole-document `decode`, chunked `decode_start` /
    `decode_continue`, deeply nested values, large arrays, large objects, and
    string-heavy inputs.
  - Record baseline numbers before parser allocation changes.
- [ ] Investigate parser state allocation and copying.
  - Measure the current immutable `Context` flow, stack handling, accumulator
    reuse, and incremental `rest` reconstruction.
  - Only introduce mutable state where the benchmark shows a real win.
- [ ] Improve parse error context.
  - Keep byte/character offset.
  - Include the parser checkpoint or expected token category.
  - Consider adding path context for nested arrays and objects.
  - Avoid exposing private implementation details such as `Checkpoint` through
    the public top-level API.
- [ ] Clarify the public package boundary between `jinser/json` and
  `jinser/json/lib`.
  - Tests still import `jinser/json/lib` to match `ParseError`.
  - Decide whether errors should be catchable through the top-level package or
    whether `lib` is intentionally public.
- [ ] Run tests in CI.
  - The current workflow runs `moon check`, `moon info --target all`, and
    `moon fmt`, but not `moon test`.
- [ ] Expand README examples.
  - Basic `decode` usage.
  - Incremental parsing.
  - Duplicate object key behavior.
  - Error handling.
  - Custom `Decode` implementations.
